name: AI新闻定时推送
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: 触发AI新闻工作流
        run: |
          curl -X POST "https://z5skwct397.coze.site/webhook_run" \
            -H "Content-Type: application/json" \
            -d '{"search_query": "AI行业最新新闻"}'
